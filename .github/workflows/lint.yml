name: lint
on:
  push: { branches: [main] }
  pull_request: { branches: [main] }
jobs:
  links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "link check ok"
